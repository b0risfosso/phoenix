const sourceEl=document.getElementById("source");async function loadScript(){const res=await fetch(`/api/script/${encodeURIComponent(filename)}`);const data=await res.json();if(!data.ok){sourceEl.textContent=data.error||"Failed to load script.";return}sourceEl.textContent=data.source||""}document.getElementById("copyBtn").addEventListener("click",async()=>{await navigator.clipboard.writeText(sourceEl.textContent||"")});loadScript();